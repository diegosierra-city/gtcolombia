import { Questrial } from "next/font/google";
import {Russo_One} from "next/font/google";

export const questrial = Questrial({ subsets: ["latin"], weight: ["400"] });
export const russo = Russo_One({ subsets: ["latin"] });